// pokemon-card.js - componente reutilizable para mostrar la tarjeta
export function renderPokemonCard(htmlString){
  // Actualmente usamos el HTML generado por el modelo; este helper
  // podría transformarlo a un DOM node o añadir wrappers adicionales.
  return htmlString;
}
